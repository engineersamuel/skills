interface:
  display_name: "Justify"
  short_description: "Audit claims with evidence and dissent"
  default_prompt: "Use $justify to audit this claim and determine whether it is justified."

dependencies:
  tools:
    - type: "mcp"
      value: "exa"
      description: "Research current external claims using Exa"
      transport: "streamable_http"
      url: "https://mcp.exa.ai/mcp"
